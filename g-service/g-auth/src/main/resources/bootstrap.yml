spring:
  application:
    # 微服务标识
    name: auth
  main:
    # 覆盖Bean
    allow-bean-definition-overriding: true
  profiles:
    active: @pom.profile.name@
#logging:
#  level:
#    org:
#      springframework:
#        security: DEBUG